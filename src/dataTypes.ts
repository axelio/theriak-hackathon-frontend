export type Epi = {
    id: number,
    text: string
}

export type EpiAbuse = {
    id: number,
    value: boolean
}

export type TrustPerson = {
    id: number,
    trustRate: number,
    name: string
}